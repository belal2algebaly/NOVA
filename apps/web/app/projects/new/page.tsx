import { Sidebar } from '../../../components/Sidebar'; import { createProject } from '../../actions/projects';
export default async function NewProject({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="shell"><Sidebar/><section className="workspace narrow"><p className="eyebrow">NEW PROJECT</p><h1>Connect a store</h1><p className="muted">Start with the store NOVA should audit and benchmark</p>{q.error&&<div className="alert error">{q.error}</div>}<form action={createProject} className="form projectForm"><label>Project name<input name="name" placeholder="Loujan Growth" required/></label><label>Store URL<input name="url" placeholder="loujan.store" required/></label><div className="formRow"><label>Store name<input name="storeName" placeholder="Loujan"/></label><label>Target market<input name="market" placeholder="Saudi Arabia"/></label></div><label>Currency<input name="currency" maxLength={3} placeholder="SAR"/></label><div className="formActions"><button className="primary">Create project</button></div></form></section></main>}
