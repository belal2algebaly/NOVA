import Link from 'next/link'; import { Brand } from '../../components/Brand'; import { signIn } from '../actions/auth'; import { SetupNotice } from '../../components/SetupNotice'; import { isSupabaseConfigured } from '../../lib/config';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string,message?:string}>}){const q=await searchParams;return <main className="authShell"><section className="authCard"><Brand/><div className="authHeading"><p className="eyebrow">WELCOME BACK</p><h1>Sign in to NOVA</h1><p>Open your projects, audits and competitor intelligence</p></div>{!isSupabaseConfigured&&<SetupNotice/>}{q.error&&<div className="alert error">{q.error}</div>}{q.message&&<div className="alert success">{q.message}</div>}<form action={signIn} className="form"><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="primary wide">Sign in</button></form><p className="authFoot">New to NOVA? <Link href="/signup">Create an account</Link></p></section></main>}
