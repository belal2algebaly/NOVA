import {NextResponse} from 'next/server';import {getCurrentUserRole} from '../../../../lib/admin';import {getSystemReport} from '../../../../lib/system/status';export async function GET(){const {user,isSuperAdmin}=await getCurrentUserRole();if(!user||!isSuperAdmin)return NextResponse.json({error:'Unauthorized'},{status:401});return NextResponse.json(await getSystemReport(),{headers:{'cache-control':'no-store'}})}
