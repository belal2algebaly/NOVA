'use server';import {revalidatePath} from 'next/cache';import {createSupabaseServerClient} from '../../lib/supabase/server';
export async function setCompetitorMonitoring(projectId:string,competitorId:string,url:string,enabled:boolean){const supabase=await createSupabaseServerClient();if(enabled){await supabase.from('monitor_targets').upsert({project_id:projectId,competitor_id:competitorId,url,enabled:true},{onConflict:'project_id,url'});}else{await supabase.from('monitor_targets').update({enabled:false}).eq('project_id',projectId).eq('competitor_id',competitorId);}revalidatePath(`/projects/${projectId}/monitoring`)}
