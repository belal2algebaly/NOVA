import Link from 'next/link'; import { Brand } from '../../components/Brand'; import { signUp } from '../actions/auth'; import { SetupNotice } from '../../components/SetupNotice'; import { isSupabaseConfigured } from '../../lib/config';
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="authShell"><section className="authCard"><Brand/><div className="authHeading"><p className="eyebrow">CREATE WORKSPACE</p><h1>Start with NOVA</h1><p>Your stores, audits and competitors live in one account</p></div>{!isSupabaseConfigured&&<SetupNotice/>}{q.error&&<div className="alert error">{q.error}</div>}<form action={signUp} className="form"><label>Name<input name="name" required autoComplete="name"/></label><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" minLength={6} required autoComplete="new-password"/><small>Minimum 6 characters</small></label><button className="primary wide">Create account</button></form><p className="authFoot">Already have an account? <Link href="/login">Sign in</Link></p></section></main>}
