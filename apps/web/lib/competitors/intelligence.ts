import type {StoreProfile} from './store-intelligence';

type Scored={match:number;confidence:number;classification:string;signals:Record<string,number|null>};
export function priceLabel(p:StoreProfile){if(!p.priceMin||!p.priceMax)return 'Not proven';const c=p.currencies?.[0]||'';return `${c?c+' ':''}${Math.round(p.priceMin).toLocaleString()} – ${Math.round(p.priceMax).toLocaleString()}`}
export function competitorInsights(own:StoreProfile,c:StoreProfile,s:Scored){const learn:string[]=[];const caveats:string[]=[];const why:string[]=[];if(own.primaryMarket&&c.primaryMarket){if(own.primaryMarket===c.primaryMarket)why.push(`Targets the same primary market: ${own.primaryMarket}`);else caveats.push(`Targets ${c.primaryMarket}, while your store is primarily ${own.primaryMarket}`)}if(s.signals.categoryOverlap!=null&&s.signals.categoryOverlap>=.5)why.push('Strong category overlap with your assortment');if(s.signals.productSimilarity!=null&&s.signals.productSimilarity>=.45)why.push('Product language is materially similar');if(s.signals.priceSimilarity!=null&&s.signals.priceSimilarity>=.7)why.push('Operates in a comparable visible price band');if(s.signals.currencyMatch===0)caveats.push('Visible store currency does not match your primary currency');if(s.signals.languageMatch!=null&&s.signals.languageMatch<.4)caveats.push('Customer language experience is materially different');if(c.categories?.length)learn.push(`Review its category structure: ${c.categories.slice(0,4).join(', ')}`);if(c.priceMedian&&own.priceMedian){if(c.priceMedian<own.priceMedian*.8)learn.push('It appears more value-led on visible pricing');else if(c.priceMedian>own.priceMedian*1.2)learn.push('It appears more premium on visible pricing');else learn.push('Its visible pricing sits close to your current position')}if(c.productSamples?.length)learn.push('Use its detected product assortment as a merchandising reference');if(!learn.length)learn.push('Use the profile as a directional reference until more evidence is collected');return {why:why.length?why:['NOVA found enough commercial similarity to keep this store in the comparison set'],caveats:caveats.length?caveats:['No major market mismatch was proven from available evidence'],learn}}
