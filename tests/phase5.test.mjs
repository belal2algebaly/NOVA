import test from 'node:test';import assert from 'node:assert/strict';import {benchmarkRuns} from '../packages/benchmark-engine/src/index.js';import {opportunitiesFromBenchmark} from '../packages/opportunity-engine/src/index.js';
const check=(key,status,weight=10)=>({key,title:key,status,weight,evidence:`${key}-${status}`,recommendation:`fix-${key}`});
test('Unknown competitor evidence is not counted as a loss',()=>{const b=benchmarkRuns({checks:[check('reviews','fail')]},[{name:'A',checks:[check('reviews','unknown')]},{name:'B',checks:[check('reviews','pass')]}]);assert.equal(b.rows[0].competitorKnown,1);assert.equal(b.rows[0].competitorPassRate,100);});
test('Unknown on your store cannot become a competitive gap',()=>{const b=benchmarkRuns({checks:[check('eta','unknown')]},[{name:'A',checks:[check('eta','pass')]},{name:'B',checks:[check('eta','pass')]}]);assert.equal(b.gaps.length,0);});
test('Known store miss plus market majority becomes a gap',()=>{const b=benchmarkRuns({checks:[check('eta','fail',12)]},[{name:'A',checks:[check('eta','pass')]},{name:'B',checks:[check('eta','pass')]},{name:'C',checks:[check('eta','fail')]}]);assert.equal(b.gaps.length,1);assert.equal(b.gaps[0].competitorPassRate,67);});
test('Opportunities come only from benchmark gaps',()=>{const b=benchmarkRuns({checks:[check('eta','fail',12)]},[{name:'A',checks:[check('eta','pass')]},{name:'B',checks:[check('eta','pass')]}]);const o=opportunitiesFromBenchmark(b);assert.equal(o.length,1);assert.match(o[0].competitorEvidence,/100%/);assert.equal(o[0].impact,'Critical');});
