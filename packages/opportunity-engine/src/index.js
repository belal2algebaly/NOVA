const impact=w=>w>=12?'Critical':w>=8?'High':w>=4?'Medium':'Low';
export function opportunitiesFromBenchmark(benchmark={}){return (benchmark.gaps||[]).map((g,i)=>{const rate=g.competitorPassRate??0;const weight=Number(g.yours?.weight||0);const confidence=Math.min(100,Math.round((g.competitorKnown>=3?85:65)+(rate>=80?10:0)));const priority=Math.round(weight*0.6+rate*0.25+confidence*0.15);return {key:g.key,title:g.title,impact:impact(weight),confidence,priority,competitorEvidence:`${rate}% of ${g.competitorKnown} comparable competitors pass this check.`,evidence:g.yours?.evidence||'Verified store finding.',recommendation:g.yours?.recommendation||'Review the competitive pattern and implement the strongest evidence-backed improvement.',status:'identified',rank:i+1};}).sort((a,b)=>b.priority-a.priority);}
export function opportunitySummary(items=[]){const open=items.filter(x=>x.status!=='dismissed');return {total:open.length,critical:open.filter(x=>x.impact==='Critical').length,high:open.filter(x=>x.impact==='High').length,top:open[0]||null};}
